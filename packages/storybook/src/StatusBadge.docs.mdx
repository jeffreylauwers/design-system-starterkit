import { Meta, Story, Controls, Markdown } from '@storybook/blocks';
import * as StatusBadgeStories from './StatusBadge.stories';
import docs from './StatusBadge.docs.md?raw';
import { PreviewFrame, CodeTabs } from './components';

export const [intro, rest] = docs.split('<!-- VOORBEELD -->');

<Meta of={StatusBadgeStories} />

<Markdown>{intro}</Markdown>

## Voorbeeld

<PreviewFrame>
  <Story of={StatusBadgeStories.Default} />
</PreviewFrame>

<CodeTabs
  of={StatusBadgeStories.Default}
  html={`<strong class="dsn-status-badge">Tekst</strong>`}
/>

<Controls of={StatusBadgeStories.Default} />

<Markdown>{rest}</Markdown>
