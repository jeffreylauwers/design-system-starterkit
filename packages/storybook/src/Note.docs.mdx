import { Meta, Story, Controls, Markdown } from '@storybook/blocks';
import * as NoteStories from './Note.stories';
import docs from './Note.docs.md?raw';
import { PreviewFrame, CodeTabs } from './components';

export const [intro, rest] = docs.split('<!-- VOORBEELD -->');

<Meta of={NoteStories} />

<Markdown>{intro}</Markdown>

## Voorbeeld

<PreviewFrame>
  <Story of={NoteStories.Default} />
</PreviewFrame>

<CodeTabs
  of={NoteStories.Default}
  html={`<div class="dsn-note">
  <span class="dsn-note__icon" aria-hidden="true">
    <svg class="dsn-icon" aria-hidden="true"><!-- info-circle --></svg>
  </span>
  <strong class="dsn-heading dsn-heading--3 dsn-note__heading">Heading</strong>
  <div class="dsn-note__content">
    <p class="dsn-paragraph">Tekst</p>
  </div>
</div>`}
/>

<Controls of={NoteStories.Default} />

<Markdown>{rest}</Markdown>
